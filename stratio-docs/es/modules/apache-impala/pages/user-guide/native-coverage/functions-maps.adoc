= _MAP_ functions

== Result

[cols="1,1,1"]
|===
|Description |Query |Implemented

| map_concat(map, ...)
| SELECT map_concat(names,city) FROM coverage_impala.mapfunctions
| No

| map_entries(map)
| SELECT map_entries(names) FROM coverage_impala.mapfunctions
| No

| map_keys(map)
| SELECT map_keys(names)  FROM coverage_impala.mapfunctions
| No

| map_values(map)
| SELECT map_values(names) FROM coverage_impala.mapfunctions
| No

| map(map)
| SELECT map(1.0, '2', 3.0, '4'),age FROM coverage_impala.mapfunctions
| No

|===
