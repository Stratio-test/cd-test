@Library('libpipelines@preproduction') _

hose {
    EMAIL = 'lgutierrez@stratio.com'
    BUILDTOOL = 'maven'
    BUILDTOOL_IMAGE = "stratio/qa-builder:0.5.0"
    INSTALLTIMEOUT = 720

    ATCREDENTIALS = [[TYPE:'sshKey', ID:'PEM_VMWARE']]

    INSTALL = { config ->
        doKeosInstall(config)
    }
}
